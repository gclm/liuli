interface:
  display_name: "Liuli"
  short_description: "Repo-specific patterns and workflows for liuli"
  default_prompt: "Use the liuli repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true